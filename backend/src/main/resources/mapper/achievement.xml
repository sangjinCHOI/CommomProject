<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.ssafy.persona.domain.character.mapper.AchievementMapper">

	<select id="getAchievementList" resultType="AchievementGetResponse">
		select a.achievement_seq, a.achievement_name, a.achievement_description, 
			a.achievement_condition, a.achievement_type, a.achievement_level, 
			a.achievement_is_active, a.achievement_created_date,
		    (select count(*) from tb_user_achievement ua where ua.achievement_seq = a.achievement_seq and ua.character_seq = 1) as isGained,
			f.file_path		
		from tb_achievement a
		LEFT OUTER JOIN tb_file f ON f.relation_tb = 'tb_achievement'
		AND f.relation_seq = a.achievement_seq
		where 1 = 1
		<if test="achievementType != null and achievementType != ''">
			and achievement_type=#{achievementType}
		</if>
		<if test="level != null and level != ''">
			and level=#{level}
		</if>
	</select>

</mapper>