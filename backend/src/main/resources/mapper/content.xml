<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.persona.content.mapper.ContentMapper">
	<insert id="contentCreate" parameterType="contentCreateRequest">
		insert into tb_content (character_seq, category_seq, content_text, content_is_public, content_is_media, content_created_date)
		values (#{characterSeq}, #{categorySeq}, #{contentText}, #{contentIsPublic}, #{contentIsMedia}, now())
	</insert>
	
	<update id="contentModify" parameterType="contentModifyRequest">
		update tb_content
		set content_text = #{contentText}, content_is_public = #{contentIsPublic}, content_is_media = #{contentIsMedia}, content_modified_date = #{contentModifiedDate}
		where content_seq = #{contentSeq}
	</update>
	
	<delete id="contentDelete" parameterType="int">
		delete from tb_content
		where content_seq = #{contentSeq}
	</delete>
	
</mapper>